@namespace MyApp.Client.Shared.Layout
@inject NavigationManager Navigation

<AuthorizeView>
    <Authorized>
        <MudAlert Severity="Severity.Error" Dense="true" Variant="Variant.Filled">
            You do not have permission to access this page.
        </MudAlert>
    </Authorized>
    <NotAuthorized>
        <MudStack Spacing="2">
            <MudAlert Severity="Severity.Warning" Dense="true" Variant="Variant.Filled">
                Your session is required to access the inventory system.
            </MudAlert>
            <MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick="NavigateToLogin">
                Go to Login
            </MudButton>
        </MudStack>
    </NotAuthorized>
</AuthorizeView>

@code {
    private void NavigateToLogin()
    {
        var relativePath = Navigation.ToBaseRelativePath(Navigation.Uri);
        var normalizedPath = string.IsNullOrWhiteSpace(relativePath) ? "/" : $"/{relativePath.TrimStart('/')}";
        var returnUrl = Uri.EscapeDataString(normalizedPath);
        Navigation.NavigateTo($"/login?returnUrl={returnUrl}", forceLoad: false);
    }
}
